#
# Pacman configuration for offline installation
# Used when the ISO contains a pre-built package repository
#

[options]
HoldPkg     = pacman glibc
Architecture = auto

CheckSpace
ParallelDownloads = 5
SigLevel    = Required DatabaseOptional
LocalFileSigLevel = Optional TrustAll

#
# Offline repository (highest priority)
#
[offline]
SigLevel = Optional TrustAll
Server = file:///opt/offline-repo

#
# No online fallback repositories
# Offline mode is truly offline - all packages must be in the offline repo
# Standard repos are restored in finish() after installation completes
