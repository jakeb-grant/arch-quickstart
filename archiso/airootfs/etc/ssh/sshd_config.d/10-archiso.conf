# SSH configuration for live environment
# Root has an empty password on the live ISO, so password auth must stay
# disabled or anyone on the network gets a root shell. For remote/headless
# use, set a root password and re-enable password auth manually, or provide
# an authorized_keys file.

PermitRootLogin prohibit-password
PasswordAuthentication no
